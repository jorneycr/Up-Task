doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            p Agrega tu e-mail para restabglecer la contraseña
            form.caja-login(method="POST" action="/restablecer")
                .campo
                    label(for="email") E-mail: 
                    input(type="text" name="email" placeholder="Tu Email" value= email)
                .campo.enviar
                    input.boton(type="submit" value="Restablecer Password")
                .campo.acciones 
                    a(href="/iniciar-sesion") Ya lo recordé, volver al Login 
